# Copy to secrets.toml and fill in your values.
# This file is read at build time to bake defaults into the firmware.
# If secrets.toml is absent, the device boots into the captive portal.

wifi_ssid = "MyNetwork"
wifi_password = "hunter2"
mqtt_broker = "mqtt://192.168.1.100:1883"
mqtt_client_id = "sliding-door"
mqtt_username = ""
mqtt_password = ""
